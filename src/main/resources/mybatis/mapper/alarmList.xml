<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="alarmList">
	<select id="getAlarmList" resultType="alarmList">
		SELECT ac_tag AS acTag, ac_comment AS acComment
		FROM tb_alarm_cmt
		WHERE ac_yn = 'y'
		ORDER BY ac_regtime DESC
		LIMIT 1
	</select>
	
	<update id="updateFileName">
	    UPDATE tb_alarm_cmt
	    SET ac_filename = #{fileName}
	    WHERE ac_regtime = #{acRegtime}
	</update>
	
	<update id="updateComment">
	    UPDATE tb_alarm_cmt
	    SET ac_comment = #{acComment}
	    WHERE ac_tag = #{acTag}
</update>
</mapper>